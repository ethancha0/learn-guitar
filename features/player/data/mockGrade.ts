import type { GradeSummary } from "../types/playback";

/** Placeholder scoring result shown before any real attempt is recorded. */
export const mockGrade: GradeSummary = {
  accuracy: 87,
  notesHit: 142,
  notesTotal: 163,
  bestStreak: 38,
};
